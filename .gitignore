# dependencies / build
node_modules/
dist/
.turbo/
*.tsbuildinfo

# environment & secrets — never commit
.env
.env.*
!.env.example

# local agent workflow (tracker-coupled, kept out of the repo on purpose)
.claude/commands/
.claude/settings.local.json

# data & runtime
data/
bots/*/memory/
*.log

# os / editors
.DS_Store
.idea/
.vscode/

# implementation-loop skill copy + local loop config (installed per machine, not part of the product)
.claude/skills/implement-spec/

# spike run output (events, screenshots, logs)
box/spike/out/
